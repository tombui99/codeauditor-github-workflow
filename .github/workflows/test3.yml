name: Test

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Manually fail the workflow
        run: |
          exit 1
      - name: Check exit code
        if: ${{ failure() }}
        run: |
          curl \
            -X POST 'https://asia-east2-sswlinkauditor-c1131.cloudfunctions.net/api/createReportIssue' \
            -H 'Content-Type: application/json' \
            -d '{"url": "https://sample.com.au", "dateReported": "$(date +"%H:%M:%S %d-%m-%Y")", "workflowId": "${{ github.run_id }}", "jobStatus": "${{ job.status }}"}' \
